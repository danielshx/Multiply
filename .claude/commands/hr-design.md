---
description: Spawn hr-architect to design or revise an HR workflow / custom tool. Pass the workflow purpose as the argument.
---

Spawn the `hr-architect` subagent with the user's request: $ARGUMENTS

Tell it: "Output the workflow spec in the standard format. Cite the brain-repo file paths you relied on. End with the JSON schemas for any new custom tools the spec introduces, ready to paste into HR Assets → Tools."
