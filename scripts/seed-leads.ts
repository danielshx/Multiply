/**
 * Seed demo leads (the 5 Live Swarm personas + 20 generic for the 25-tile grid).
 *
 * Run with: npx tsx scripts/seed-leads.ts
 *
 * Brain-repo: README.md lines 122–132 (personas), line 323 ("25 Agents").
 */
async function main() {
  console.log("[seed-leads] stub — implement against Supabase.");
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
